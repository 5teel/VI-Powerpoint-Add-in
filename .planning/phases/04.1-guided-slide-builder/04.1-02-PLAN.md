---
phase: 04.1-guided-slide-builder
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - src/taskpane/components/App.tsx
  - src/taskpane/components/WizardPanel.tsx
  - src/taskpane/components/wizard/BrandStep.tsx
  - src/taskpane/components/wizard/ImageStep.tsx
  - src/taskpane/components/wizard/PurposeStep.tsx
  - src/taskpane/components/wizard/ReviewStep.tsx
  - src/taskpane/components/wizard/StepIndicator.tsx
autonomous: false
requirements:
  - SC-01
  - SC-02
  - SC-03
  - SC-04
  - SC-05

must_haves:
  truths:
    - "User can select the Build Slide tab and see a wizard with step 1 active"
    - "User can type a brand name and advance to step 2"
    - "User can upload a PNG/JPG image and see a thumbnail preview"
    - "User can choose an analysis purpose and advance to step 4"
    - "User can review brand, purpose, and image thumbnail before building"
    - "User clicks Build Slide and a slide is created in PowerPoint with the product image"
    - "Switching tabs preserves wizard state (display:none, not unmount)"
  artifacts:
    - path: "src/taskpane/components/WizardPanel.tsx"
      provides: "Main wizard container with step state machine"
      min_lines: 80
    - path: "src/taskpane/components/wizard/BrandStep.tsx"
      provides: "Brand name free text input"
      contains: "brandName"
    - path: "src/taskpane/components/wizard/ImageStep.tsx"
      provides: "Image upload with preview"
      contains: "validateImageFile"
    - path: "src/taskpane/components/wizard/PurposeStep.tsx"
      provides: "Analysis purpose selection"
      contains: "purpose"
    - path: "src/taskpane/components/wizard/ReviewStep.tsx"
      provides: "Review card with Build Slide button"
      contains: "Build Slide"
    - path: "src/taskpane/components/wizard/StepIndicator.tsx"
      provides: "Progress indicator showing current step"
      contains: "step"
    - path: "src/taskpane/components/App.tsx"
      provides: "Third tab for wizard"
      contains: "wizard"
  key_links:
    - from: "src/taskpane/components/wizard/ImageStep.tsx"
      to: "src/taskpane/services/imageUtils.ts"
      via: "import validateImageFile, resizeImageToBase64"
      pattern: "import.*imageUtils"
    - from: "src/taskpane/components/wizard/ReviewStep.tsx"
      to: "src/taskpane/services/promptBuilder.ts"
      via: "import buildGuidedPrompt"
      pattern: "buildGuidedPrompt"
    - from: "src/taskpane/components/WizardPanel.tsx"
      to: "src/taskpane/services/slideRenderer.ts"
      via: "insertSlide(content, productImageBase64)"
      pattern: "insertSlide.*productImageBase64"
    - from: "src/taskpane/components/App.tsx"
      to: "src/taskpane/components/WizardPanel.tsx"
      via: "display:none tab rendering"
      pattern: "display.*wizard"
---

<objective>
Build the WizardPanel UI with all 4 steps (Brand, Image, Purpose, Review), integrate as a third tab in App.tsx using display:none for state preservation, and wire the Review step to the existing slide creation pipeline.

Purpose: Delivers the complete guided slide builder experience — the user-facing feature for Phase 4.1.
Output: WizardPanel component, 4 step components, StepIndicator, updated App.tsx with third tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-guided-slide-builder/04.1-CONTEXT.md
@.planning/phases/04.1-guided-slide-builder/04.1-RESEARCH.md
@.planning/phases/04.1-guided-slide-builder/04.1-01-SUMMARY.md

<interfaces>
<!-- Interfaces from Plan 01 that this plan consumes -->

From src/taskpane/services/imageUtils.ts (created in Plan 01):
```typescript
export function validateImageFile(file: File): string | null;
export function resizeImageToBase64(file: File): Promise<string>;
```

From src/taskpane/services/promptBuilder.ts (extended in Plan 01):
```typescript
export function buildSlidePrompt(userQuestion: string): string;
export function buildGuidedPrompt(brandName: string, purpose: string): string;
```

From src/taskpane/services/slideRenderer.ts (extended in Plan 01):
```typescript
export async function insertSlide(content: SlideContent, productImageBase64?: string): Promise<void>;
```

From src/taskpane/slide/types.ts (extended in Plan 01):
```typescript
export interface WizardData {
  brandName: string;
  productImageBase64: string | null;
  purpose: string;
}
export type WizardStep = 1 | 2 | 3 | 4;
export type BuildState = "idle" | "building" | "built" | "failed";
```

From src/taskpane/services/cubeai.ts (existing):
```typescript
export function streamCubeAI(question: string, chatId: string | null, callbacks: StreamCallbacks): AbortController;
```

From src/taskpane/services/schemaParser.ts (existing):
```typescript
export function extractSlideContent(raw: string): SlideContent | null;
export function fallbackToTextOnly(text: string): TextOnlyContent;
```

From src/taskpane/components/App.tsx (current):
```typescript
type TabValue = "slides" | "chat";
// Renders tabs with conditional rendering: {activeTab === "chat" ? <ChatPanel /> : <SlideTestPanel />}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WizardPanel container and step components</name>
  <files>
    src/taskpane/components/WizardPanel.tsx
    src/taskpane/components/wizard/StepIndicator.tsx
    src/taskpane/components/wizard/BrandStep.tsx
    src/taskpane/components/wizard/ImageStep.tsx
    src/taskpane/components/wizard/PurposeStep.tsx
    src/taskpane/components/wizard/ReviewStep.tsx
  </files>
  <read_first>
    src/taskpane/components/ChatPanel.tsx
    src/taskpane/components/App.tsx
    src/taskpane/services/imageUtils.ts
    src/taskpane/services/promptBuilder.ts
    src/taskpane/services/slideRenderer.ts
    src/taskpane/slide/types.ts
    .planning/phases/04.1-guided-slide-builder/04.1-CONTEXT.md
    .planning/phases/04.1-guided-slide-builder/04.1-RESEARCH.md
  </read_first>
  <action>
    **Create src/taskpane/components/wizard/ directory.**

    **Create src/taskpane/components/wizard/StepIndicator.tsx** (Claude's discretion — numbered step dots):
    - Props: `{ currentStep: WizardStep; }`
    - Render 4 numbered circles (1-4) with labels: "Brand", "Image", "Purpose", "Review"
    - Current step: filled with SUMMIT_NAVY (#0F1330), white text
    - Completed steps (step < currentStep): filled with lighter blue (#2563EB), white text
    - Future steps: gray border, gray text
    - Use flexbox row with small connecting lines between circles
    - Keep compact — the taskpane is narrow (~320px)

    **Create src/taskpane/components/wizard/BrandStep.tsx** (per D-08):
    - Props: `{ brandName: string; onBrandChange: (name: string) => void; }`
    - Fluent UI `Input` with placeholder "e.g., Coca Cola, Red Bull..."
    - Label: "What brand should we focus on?"
    - Brief helper text below input: "Enter the brand name for your analysis"
    - Use Fluent UI `Text` component for labels, consistent with ChatPanel styling
    - Style: padding 16px, gap 12px between elements

    **Create src/taskpane/components/wizard/ImageStep.tsx** (per D-04, D-05, D-06):
    - Props: `{ imageBase64: string | null; onImageSelected: (base64: string) => void; }`
    - Hidden `<input type="file" accept="image/png,image/jpeg,.png,.jpg,.jpeg">` (per Pitfall 6)
    - Fluent UI `Button` "Upload Product Image" that triggers file input click
    - On file change: call `validateImageFile(file)` — if error, show error text in red
    - If valid: call `resizeImageToBase64(file)`, pass result to onImageSelected
    - Show loading spinner during resize
    - When imageBase64 exists: show thumbnail `<img src={\`data:image/png;base64,${imageBase64}\`} />` with max-height 200px, border-radius 8px
    - "Replace image" button appears below thumbnail to re-trigger upload
    - Import validateImageFile and resizeImageToBase64 from `../../services/imageUtils`

    **Create src/taskpane/components/wizard/PurposeStep.tsx** (Claude's discretion — predefined options + custom):
    - Props: `{ purpose: string; onPurposeChange: (purpose: string) => void; }`
    - Label: "What type of analysis?"
    - Render 4-5 clickable option cards (not a dropdown):
      - "Range Review" — "Analyze product range performance and distribution"
      - "Performance Breakdown" — "Deep dive into sales and growth metrics"
      - "Competitor Comparison" — "Compare against key competitors"
      - "Category Overview" — "High-level category trends and insights"
      - "Custom" — shows a text input when selected
    - Selected option: highlighted border with SUMMIT_NAVY, light blue background
    - Unselected: gray border, white background
    - Each card: padding 10px, border-radius 8px, cursor pointer
    - When "Custom" is selected, render an Input below for free text

    **Create src/taskpane/components/wizard/ReviewStep.tsx** (per D-10, D-11):
    - Props: `{ data: WizardData; buildState: BuildState; onBuild: () => void; onReset: () => void; }`
    - Render a review card showing:
      - Brand: `data.brandName` in bold
      - Purpose: `data.purpose`
      - Image: thumbnail of `data.productImageBase64` (same display pattern as ImageStep)
    - Card styling: border-radius 12px, subtle border, padding 16px
    - "Build Slide" button (Fluent UI Button, appearance="primary", backgroundColor SUMMIT_NAVY)
    - Button states (same pattern as ChatPanel Create Slide):
      - buildState === "idle": enabled "Build Slide" button
      - buildState === "building": disabled button with Spinner + "Building..."
      - buildState === "built": green checkmark text "Slide created!" + "Start Over" button calling onReset
      - buildState === "failed": red error text + "Try Again" button (resets to idle)

    **Create src/taskpane/components/WizardPanel.tsx** (per D-02, D-03):
    - Import all step components, StepIndicator, WizardData/WizardStep/BuildState from types
    - State:
      - `step: WizardStep` — starts at 1
      - `data: WizardData` — `{ brandName: "", productImageBase64: null, purpose: "" }`
      - `buildState: BuildState` — starts at "idle"
    - `canAdvance()` logic:
      - Step 1: `data.brandName.trim().length > 0`
      - Step 2: `data.productImageBase64 !== null`
      - Step 3: `data.purpose.trim().length > 0`
      - Step 4: always true
    - Navigation buttons at bottom:
      - "Back" (disabled on step 1, calls `setStep(s => (s - 1) as WizardStep)`)
      - "Next" (hidden on step 4, disabled if !canAdvance(), calls `setStep(s => (s + 1) as WizardStep)`)
    - Step rendering via switch statement (not conditional && chains)
    - `handleBuild` async function:
      - Set buildState to "building"
      - Call `buildGuidedPrompt(data.brandName, data.purpose)` to get the prompt
      - Call `streamCubeAI(prompt, null, { onComplete, onError })` — note: chatId is null (single-shot)
      - In onComplete: call `extractSlideContent(result.content)` to parse, then `insertSlide(content, data.productImageBase64)` to create slide with image
      - On success: set buildState to "built"
      - On error: set buildState to "failed"
    - `handleReset` function: reset step to 1, data to defaults, buildState to "idle"
    - Layout: flex column, StepIndicator at top, step content fills middle, navigation buttons pinned at bottom
    - Consistent styling with ChatPanel: same padding, font sizes, color scheme
  </action>
  <verify>
    <automated>cd "C:/Development/Integration/Powerpoint" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "WizardPanel" src/taskpane/components/WizardPanel.tsx` confirms component exists
    - `grep "BrandStep" src/taskpane/components/wizard/BrandStep.tsx` confirms component exists
    - `grep "ImageStep" src/taskpane/components/wizard/ImageStep.tsx` confirms component exists
    - `grep "PurposeStep" src/taskpane/components/wizard/PurposeStep.tsx` confirms component exists
    - `grep "ReviewStep" src/taskpane/components/wizard/ReviewStep.tsx` confirms component exists
    - `grep "StepIndicator" src/taskpane/components/wizard/StepIndicator.tsx` confirms component exists
    - `grep "validateImageFile" src/taskpane/components/wizard/ImageStep.tsx` confirms import
    - `grep "resizeImageToBase64" src/taskpane/components/wizard/ImageStep.tsx` confirms import
    - `grep "buildGuidedPrompt" src/taskpane/components/WizardPanel.tsx` confirms prompt builder usage
    - `grep "insertSlide" src/taskpane/components/WizardPanel.tsx` confirms slide creation call
    - `grep "productImageBase64" src/taskpane/components/WizardPanel.tsx` confirms image passed to insertSlide
    - `grep "streamCubeAI" src/taskpane/components/WizardPanel.tsx` confirms pipeline reuse
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>
    - WizardPanel renders 4-step wizard with step state machine
    - BrandStep accepts free text brand name (per D-08)
    - ImageStep uploads PNG/JPG with validation, resize, and thumbnail preview (per D-04, D-05, D-06)
    - PurposeStep offers predefined options plus custom text
    - ReviewStep shows summary card with Build Slide button and state lifecycle (per D-10, D-11)
    - Build Slide calls streamCubeAI -> extractSlideContent -> insertSlide(content, productImageBase64) pipeline (per D-03)
    - TypeScript compiles cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate WizardPanel as third tab in App.tsx</name>
  <files>
    src/taskpane/components/App.tsx
  </files>
  <read_first>
    src/taskpane/components/App.tsx
    src/taskpane/components/WizardPanel.tsx
    .planning/phases/04.1-guided-slide-builder/04.1-CONTEXT.md
  </read_first>
  <action>
    **Modify src/taskpane/components/App.tsx** (per D-01, Pitfall 5):

    1. Add import: `import WizardPanel from "./WizardPanel";`

    2. Extend TabValue type: `type TabValue = "slides" | "chat" | "wizard";`

    3. Add third tab in TabList (after "Slide Options"):
       ```tsx
       <Tab value="wizard">Build Slide</Tab>
       ```

    4. **Replace conditional rendering with display:none pattern** (Pitfall 5 — prevents state loss on tab switch):
       Replace the existing `{activeTab === "chat" ? <ChatPanel /> : <SlideTestPanel />}` with:
       ```tsx
       <div style={{ display: activeTab === "chat" ? "flex" : "none", flexDirection: "column", flex: 1, overflow: "hidden" }}>
         <ChatPanel />
       </div>
       <div style={{ display: activeTab === "slides" ? "flex" : "none", flexDirection: "column", flex: 1, overflow: "hidden" }}>
         <SlideTestPanel />
       </div>
       <div style={{ display: activeTab === "wizard" ? "flex" : "none", flexDirection: "column", flex: 1, overflow: "hidden" }}>
         <WizardPanel />
       </div>
       ```
       This keeps all three components mounted at all times but only the active one visible. Wizard state (brand, image, purpose) is preserved when switching tabs.

    5. Keep the existing container div styling unchanged (flex column, border-radius 12px, etc.)
  </action>
  <verify>
    <automated>cd "C:/Development/Integration/Powerpoint" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "wizard" src/taskpane/components/App.tsx` finds the tab value
    - `grep "Build Slide" src/taskpane/components/App.tsx` finds the tab label
    - `grep "WizardPanel" src/taskpane/components/App.tsx` finds the component usage
    - `grep "display.*none" src/taskpane/components/App.tsx` confirms display:none pattern (not conditional unmount)
    - `grep -c "display:" src/taskpane/components/App.tsx` returns >= 3 (one per tab panel)
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>
    - App.tsx has three tabs: "Ask Summit", "Slide Options", "Build Slide" (per D-01)
    - All three panels rendered with display:none/flex — no unmounting on tab switch (Pitfall 5)
    - WizardPanel imported and rendered in the wizard tab
    - TypeScript compiles cleanly
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify guided slide builder in PowerPoint</name>
  <files>
    src/taskpane/components/App.tsx
    src/taskpane/components/WizardPanel.tsx
  </files>
  <action>
    No code changes — this is a manual verification checkpoint. The executor should ensure the dev server is running and guide the user through the verification steps below.
  </action>
  <verify>
    <automated>cd "C:/Development/Integration/Powerpoint" && npx tsc --noEmit --pretty 2>&1 | head -5</automated>
  </verify>
  <done>
    User has verified the full wizard flow works in PowerPoint: brand entry, image upload with preview, purpose selection, review card, slide creation with product image, and tab switching preserves state.
  </done>
  <what-built>
    Complete 4-step guided slide builder wizard as a "Build Slide" tab in the PowerPoint add-in. The wizard guides through Brand selection, Image upload, Analysis purpose, and Review/Build steps. Creates a data-driven slide with the uploaded product image.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev-server`
    2. Open PowerPoint and load the add-in via sideload
    3. Verify three tabs appear: "Ask Summit", "Slide Options", "Build Slide"
    4. Click "Build Slide" tab — wizard should show Step 1 (Brand) with numbered step indicator at top

    **Step 1 - Brand:**
    5. Type "Red Bull" in the brand input field
    6. Verify "Next" button becomes enabled
    7. Click "Next"

    **Step 2 - Image:**
    8. Click "Upload Product Image" button
    9. Select a PNG or JPG image file from your computer
    10. Verify the image thumbnail appears in the wizard
    11. Click "Next"

    **Step 3 - Purpose:**
    12. Click one of the purpose options (e.g., "Range Review")
    13. Verify the option highlights
    14. Click "Next"

    **Step 4 - Review:**
    15. Verify the review card shows: brand name "Red Bull", purpose "Range Review", and image thumbnail
    16. Click "Build Slide"
    17. Verify a loading state appears ("Building...")
    18. Wait for Cube AI response (5-15 seconds)
    19. Verify a new slide is created in the presentation with data content AND the uploaded product image

    **Tab preservation test:**
    20. Go back to Step 1, enter a brand name, switch to "Ask Summit" tab, switch back to "Build Slide"
    21. Verify the brand name is still there (state was preserved)

    **Expected result:** The full wizard flow works, a slide with product image is created, and tab switching preserves wizard state.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- All existing tests pass: `npx vitest --run`
- Wizard is accessible from "Build Slide" tab
- Full wizard flow produces a slide with product image in PowerPoint
- Tab switching preserves wizard state
</verification>

<success_criteria>
- Three tabs visible in the add-in: Ask Summit, Slide Options, Build Slide
- 4-step wizard guides user through Brand, Image, Purpose, Review
- Image upload validates PNG/JPG only, resizes to 800px max, shows preview
- Build Slide creates a PowerPoint slide with data content and product image
- Tab switching does not lose wizard state
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-guided-slide-builder/04.1-02-SUMMARY.md`
</output>
