---
phase: 04.2-deployment
plan: 01
subsystem: infra
tags: [docker, nginx, webpack, railway, defineplugin, deployment]

# Dependency graph
requires:
  - phase: 04.1-guided-workflow
    provides: complete add-in codebase ready for deployment packaging
provides:
  - DefinePlugin-based config injection replacing hardcoded secrets
  - Multi-stage Docker image (node build + nginx serve)
  - Railway deployment configuration (railway.toml)
  - Production manifest with Railway domain URLs
affects: [04.2-deployment plan 02 (Railway deploy + sideload)]

# Tech tracking
tech-stack:
  added: [webpack.DefinePlugin, nginx:alpine-slim, node:22-alpine, railway.toml]
  patterns: [build-time env injection via DefinePlugin, multi-stage Docker build, dynamic PORT via envsubst]

key-files:
  created: [Dockerfile, nginx.conf, .dockerignore, railway.toml]
  modified: [src/taskpane/config.ts, webpack.config.js, .gitignore, manifest.xml, vitest.config.ts]

key-decisions:
  - "DefinePlugin with JSON.stringify for all values -- literal text replacement requires stringification"
  - "npm ci --ignore-scripts in Docker to avoid sharp native module build errors on Alpine"
  - "envsubst for dynamic PORT injection at container startup (Railway requirement)"
  - "Default fallback URLs in DefinePlugin for local dev without env vars"

patterns-established:
  - "Build-time config injection: env vars -> DefinePlugin -> global constants -> config.ts"
  - "Docker multi-stage: node:22-alpine build, nginx:alpine-slim serve"
  - "Railway PORT pattern: envsubst in CMD replaces $PORT in nginx.conf template"

requirements-completed: []

# Metrics
duration: 4min
completed: 2026-03-30
---

# Phase 04.2 Plan 01: Railway Deployment Preparation Summary

**DefinePlugin build-time config injection, multi-stage Docker image (node+nginx), and Railway deployment config replacing all hardcoded secrets**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-30T04:39:18Z
- **Completed:** 2026-03-30T04:43:14Z
- **Tasks:** 3
- **Files modified:** 9

## Accomplishments
- Removed hardcoded API key from config.ts, replaced with DefinePlugin build-time injection from env vars
- Created complete Docker infrastructure: multi-stage Dockerfile, nginx.conf with dynamic PORT, .dockerignore, railway.toml
- Production webpack build verified: zero localhost references in dist/manifest.xml, 9 Railway URLs
- All 43 existing tests pass with DefinePlugin globals added to vitest config

## Task Commits

Each task was committed atomically:

1. **Task 1: Refactor config.ts to DefinePlugin globals, update webpack and manifest** - `ec8f1a9` (feat)
2. **Task 2: Create Docker infrastructure files** - `5904756` (feat)
3. **Task 3: Verify production build and fix test compatibility** - `9415d73` (fix)

## Files Created/Modified
- `src/taskpane/config.ts` - Refactored to read DefinePlugin globals instead of hardcoded values
- `webpack.config.js` - Added DefinePlugin with 4 env var mappings, updated urlProd to Railway URL
- `.gitignore` - Added .planning/, config.ts.txt, .claude/ exclusions
- `manifest.xml` - Updated AppDomains and SupportUrl to Railway domain
- `Dockerfile` - Multi-stage build: node:22-alpine (build) + nginx:alpine-slim (serve)
- `nginx.conf` - Dynamic PORT via envsubst, serves taskpane.html, caches static assets
- `.dockerignore` - Excludes node_modules, .git, .planning, .claude, secrets
- `railway.toml` - DOCKERFILE builder with health check and restart policy
- `vitest.config.ts` - Added DefinePlugin globals for test environment compatibility

## Decisions Made
- Used `JSON.stringify()` for all DefinePlugin values (required for literal text replacement)
- Default fallback URLs in DefinePlugin so local dev works without env vars set
- `npm ci --ignore-scripts` in Docker to avoid sharp native module build issues on Alpine
- envsubst at container CMD time for Railway's dynamic PORT injection

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added DefinePlugin globals to vitest.config.ts**
- **Found during:** Task 3 (production build verification)
- **Issue:** vitest failed with ReferenceError: __CUBEAI_BASE_URL__ is not defined -- config.ts globals not available in test environment
- **Fix:** Added `define` block to vitest.config.ts with test values for all 4 globals
- **Files modified:** vitest.config.ts
- **Verification:** All 43 tests pass
- **Committed in:** 9415d73 (Task 3 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Auto-fix was anticipated in the plan's fallback instructions. No scope creep.

## Issues Encountered
- Docker daemon not running in worktree environment -- Docker image build could not be verified locally. Dockerfile syntax and structure are correct; actual build will be verified during Railway deployment (Plan 02).

## User Setup Required

None - no external service configuration required for this plan. Railway env vars will be configured in Plan 02.

## Next Phase Readiness
- Production webpack build produces correct output with env var injection
- Docker infrastructure files ready for Railway deployment
- Plan 02 can proceed with Railway CLI setup, env var configuration, and deployment

---
*Phase: 04.2-deployment*
*Completed: 2026-03-30*
