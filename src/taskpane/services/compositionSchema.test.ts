import { describe, it } from "vitest";

describe("CompositionPlanSchema", () => {
  it.todo("CMPS-01: accepts a minimal valid plan (layout/regions/title/commentary)");
  it.todo("CMPS-01: rejects a plan missing commentary");
  it.todo("CMPS-02: rejects a region with x+w > 1");
  it.todo("CMPS-02: rejects a region with y+h > 1");
  it.todo("CMPS-01: rejects layout outside enum");
});
