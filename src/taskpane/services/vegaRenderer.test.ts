import { describe, it } from "vitest";

describe("vegaRenderer", () => {
  it.todo("CHRT-01: compiles Vega-Lite bar spec via vegaLite.compile(spec).spec");
  it.todo("CHRT-02: compiles Vega-Lite line spec");
  it.todo("CHRT-03: compiles Vega-Lite pie (arc) spec");
  it.todo("CHRT-04: returns raw base64 with no 'data:' prefix (strip via split(',')[1])");
});
